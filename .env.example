# Copy to .env.local and fill in with values from your Supabase project
# (Project Settings > API). .env.local is gitignored; never commit real values.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=